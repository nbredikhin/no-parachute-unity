using UnityEngine;
using System.Collections;

public class Idle : BaseMovement 
{    
    public override void Setup(Vector3 MovementSpeed, float RotationSpeed)
    {
        base.Setup(MovementSpeed, RotationSpeed);
    }
}
